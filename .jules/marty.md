## 2026-09-15 - Auth Form Submitting States & Loading UX **Learning:** Asynchronous frontend form submissions across `Login`, `Register`, `ForgotPassword`, and `ResetPassword` should manage submission loading state (`isSubmitting`) explicitly to prevent double-submissions, disable inputs/links during pending API calls, and re-enable form controls cleanly in a `finally` block upon error or response. **Action:** Future Marty runs modifying interactive forms or API client interactions should enforce `isSubmitting` controls and update corresponding unit tests to verify disabled states and failure recovery.
